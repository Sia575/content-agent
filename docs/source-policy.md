# Source policy

RSS source definitions live in `config/settings.yaml`. Prefer official announcements and established editorial outlets. Each final line links to its original source; the MVP treats each source item as a reported claim and does not independently verify it.

Before production use, review feed availability, licensing, and the regional classification of each source.
